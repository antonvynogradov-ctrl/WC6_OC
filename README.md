# Звіт: Адміністрування користувачів та керування командними інтерпретаторами в GNU/Linux
**Виконав:** Перевишко Денис

---

## 1. Підготовка середовища та розширення можливостей системи

Для виконання завдання було використано хмарне середовище **LabEx**. Першочерговим завданням стало розширення функціоналу системи шляхом встановлення альтернативних командних інтерпретаторів, що відрізняються від стандартного `bash` логікою автодоповнення та візуалізацією.

| Інтерпретатор | Команда встановлення | Особливості |
|:---:|:---|:---|
| **Zsh** | `sudo apt install zsh` | Підтримка потужних фреймворків (Oh My Zsh), покращена робота з плагінами. |
| **Fish** | `sudo apt install fish` | "Friendly Interactive Shell" — має розумну підсвітку синтаксису "з коробки". |

**Процес оновлення репозиторіїв та інсталяції пакетів:**
```bash
sudo apt update && sudo apt install zsh fish -y
2. Проектування структури користувачів та групВідповідно до технічного завдання, у системі було розгорнуто ієрархію з 5 професійних груп. Для кожної групи визначено специфічні права доступу до командного інтерфейсу.Таблиця конфігурації облікових записів:ГрупаКількість користувачівПризначений ShellДоступ до CLITechnical support2/bin/bashДозволеноDevelopers2/usr/bin/zshДозволеноFounders2/usr/bin/fishДозволеноFinanciers2nologinЗаблокованоGuests2nologinЗаблоковано3. Технічна реалізація (Командний блок)Етапи створення груп та автоматизованого додавання користувачів з прив'язкою до відповідних домашніх директорій та оболонок:Bash# Створення системних груп
sudo groupadd tech_support && sudo groupadd developers && sudo groupadd financiers && sudo groupadd founders && sudo groupadd guests

# Масове створення користувачів (приклад для основних груп)
sudo useradd -m -g tech_support -s /bin/bash tech1
sudo useradd -m -g developers -s /usr/bin/zsh dev1
sudo useradd -m -g founders -s /usr/bin/fish boss1
sudo useradd -m -g financiers -s /usr/sbin/nologin fin1
sudo useradd -m -g guests -s /usr/sbin/nologin guest1
Статус перевірки: Всі 10 записів успішно внесені до файлу /etc/passwd.<p align="center"><a href="ТВОЄ_ПОСИЛАННЯ_НА_IMGBB"><img src="ТВОЄ_ПОСИЛАННЯ_НА_КАРТИНКУ" alt="Конфігурація passwd" border="0" width="80%"></a></p>4. Верифікація роботи та аналіз інтерфейсівДля підтвердження коректності налаштувань було проведено імітацію входу під різними обліковими записами та збір системних метрик.КористувачДія (Команда)Результат аналізуtech1sudo su - tech1Стандартне середовище Bash, успішний вивід date.dev1sudo su - dev1Активація Zsh, розширений prompt.boss1sudo su - boss1Активація Fish, перевірка ресурсів через free -h.fin1sudo su - fin1Спрацювання тригера nologin. Вхід відхилено.Візуалізація роботи у різних оболонках:<p align="center"><img src="ПОСИЛАННЯ_НА_СКРИНШОТ_BASH" width="30%" title="Bash Session" /><img src="ПОСИЛАННЯ_НА_СКРИНШОТ_ZSH" width="30%" title="Zsh Session" /><img src="ПОСИЛАННЯ_НА_СКРИНШОТ_FISH" width="30%" title="Fish Session" /></p>ВисновкиВ ході виконання Work-case 6 було реалізовано комплексну схему керування локальними користувачами. Основні досягнення:Диференціація середовищ: Різні відділи отримали оптимізовані під їх завдання інтерпретатори (Zsh для розробників, Fish для керівництва).Політика безпеки: Реалізовано механізм обмеження доступу для груп Financiers та Guests шляхом призначення оболонки-заглушки /usr/sbin/nologin.Автоматизація: Продемонстровано можливість швидкого розгортання структури організації через CLI.
